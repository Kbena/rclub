{% extends 'base.html.twig' %}

{% block body %}

{% if error %}
    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
{% endif %}
    <main class="main-log">
    
        {# <div class="prog">
            <h2>Connectez Vous Ici <i class="fab fa-ethereum"></i></h2>
        </div>
        <br> #}

        {#
        <form action="{{ path('security_login') }}" method="post">
            <div class="form-group">
                <i class="fas fa-at"></i> Email
                <input type="text" class="form-control" name="_username" 
                placeholder="Votre Email" required>
            </div>
            <div class="form-group">
                <i class="fas fa-unlock-alt"></i> Mot de Passe
                <input type="password" class="form-control" name="_password" 
                placeholder="Votre Mot de Passe" required> 
            </div>
            <div>
                <button type="submit" class="btn btn-success float-right">Connexion</button>
            </div>

        </form> #}
        <!-- Material form subscription -->
            <div class="card">
                <h2 class="card-header white-text text-center py-4 tit-con-form">
                    <strong>Connexion</strong>
                </h2>

                <!--Card content-->
                <div class="card-body px-lg-5">
                    <!-- Form -->
                    <form class="text-center" action="{{ path('security_login') }}" method="post" style="color: #757575;">
                        <p>Join our mailing list. We write rarely, but only the best content.</p>
                        <p>
                            <a href="" target="_blank">See the last newsletter</a>
                        </p>                 
                        <!-- E-mai -->
                        <div class="md-form">
                            <input type="email" id="materialSubscriptionFormEmail" class="form-control" name="_username">
                            <label for="materialSubscriptionFormEmail">E-mail</label>
                        </div>
                        <!-- Name -->
                        <div class="md-form mt-3">
                            <input type="password" id="materialSubscriptionFormPasswords" class="form-control" name="_password">
                            <label for="materialSubscriptionFormPasswords">Passwords</label>
                        </div>
                        <!-- Sign in button -->
                        <button class="btn btn-outline-info btn-rounded btn-block z-depth-0 my-4 waves-effect" type="submit">Sign in</button>
                    </form>
                </div>

            </div>
            <!-- Material form subscription -->
    </main>

{% endblock %}